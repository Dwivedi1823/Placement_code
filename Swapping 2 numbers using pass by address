#include <bits/stdc++.h>
using namespace std;

void swp(int *a,int *b)
{
    int temp;
    temp = *a;
    *a = *b;
    *b = temp;
}

int main()
{
    int a = 4,b = 5;
    swp(&a,&b);
    cout<<a<<" "<<b;
    return 0;
}
